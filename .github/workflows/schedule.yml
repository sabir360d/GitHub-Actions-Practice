name: Scheduled Job

on:
  schedule:
    - cron: '0 0 * * *'

jobs:
  scheduled-job:
    runs-on: ubuntu-latest

    steps:
      - name: Run scheduled task
        run: echo "Running daily at midnight UTC"